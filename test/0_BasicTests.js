const QVF = artifacts.require("QVFactory");
const TC = artifacts.require("TokenContract");


contract("Base Setup Checks", async accounts => {
  before(async function() {
    // Setup

  });

  after(async function() {
    console.log("Test Finished");
  });

  describe("Basic Setup - Stage 1", function() {
    describe("Basic Setup - Stage 1.1", function () {
      it('Basic Check to Check deployement status', async function () {

      })
    })
  })
})
